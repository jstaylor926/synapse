/**
 * @synapse/ui-kit — shared React components and design-system primitives
 * consumed by the cockpit and other React surfaces.
 *
 * Components land here as the design system grows. Re-export them from this
 * barrel so surfaces import from a single entry point:
 *
 *   import { Button } from "@synapse/ui-kit";
 */

export {};
