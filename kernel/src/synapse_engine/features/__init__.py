"""In-process kernel features (spaced repetition, planner, capture, …).

Ported from the legacy tree one capability at a time (M0). `sr` (FSRS-6 spaced
repetition over a local SQLite store) is the first to land.
"""
