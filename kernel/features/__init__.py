"""In-process capabilities.

Each module here is a self-contained capability the edges compose:

- `pdf`     — PDF -> Markdown conversion.
- `sr`      — FSRS-6 spaced repetition.
- `capture` — quick capture into the vault (notes, highlights, audio memos).
- `planner` — task/ontology scheduling against real-world deadlines.
"""
